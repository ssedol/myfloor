# PRD — 몇층 (MyFloor)
**버전**: v0.3  
**작성일**: 2026-06-09  
**작성자**: 쌔돌  
**상태**: 개발 착수 완료

---

## 변경 이력
| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | 2026-06-08 | 초안 작성 |
| v0.2 | 2026-06-08 | 도메인 확정, 컬러 시스템 추가, 환경변수 처리 방식 추가 |
| v0.3 | 2026-06-09 | UX 엣지케이스 확정, 차량 최대 3대, 폰트/아이콘 방식 확정 |

---

## 1. 서비스 개요

### 배경
아파트 지하 주차장에 주차 후 몇 층에 주차했는지 기억하지 못하는 문제가 빈번하게 발생한다. 기존 해결책(메모 앱, 연락처 저장 등)은 마찰이 크고 잊기 쉽다.

### 목적
- NFC 태그를 활용해 주차 즉시 층수를 저장하는 마찰 없는 UX 제공
- 아파트 주민 누구나 별도 회원가입 없이 즉시 사용
- 향후 타 아파트 단지에 화이트라벨로 확장 가능한 구조 설계

### 서비스명
- 한글: **몇층**
- 영문: **MyFloor**
- 도메인: **myfloor.app** (Google Registry 운영 gTLD, HTTPS 강제 적용)

---

## 2. 타겟 사용자

### MVP (v1.0)
- 특정 단일 아파트 단지 주민
- 지하 주차장을 이용하는 모든 세대
- 차량 1대 이상 보유 세대

### 향후 확장
- 다수의 아파트 단지 (화이트라벨 B2B)
- 지하 주차장 보유 오피스텔, 상업용 건물

---

## 3. 핵심 기능 (MVP v1.0)

### 3-1. 차량 관리 (메인 화면 인라인)
- 차량 등록: 이름 직접 입력 (예: "아빠차", "엄마차")
- 차량 삭제 (더블탭 확인 후 삭제)
- 차량 이름 편집 (편집 아이콘 → 모달)
- **최대 3대까지 등록 가능**
- 등록된 차량 목록 표시 + 각 차량의 현재 저장된 층수 표시

### 3-2. NFC 태그로 층수 저장 (주요 입력 방식)
- 각 지하 주차층 엘리베이터 앞에 NFC 태그 부착
- 태그 URL 형식: `https://myfloor.app/park?floor=B3`
- 동작 흐름:
  1. 주차 후 엘리베이터 탑승 전 NFC 태그에 폰 태그
  2. 앱 자동 실행 (미설치 시 브라우저로 열림)
  3. 차량 선택 팝업 표시 (2대 이상인 경우) / 자동 저장 (1대인 경우)
  4. 차량 선택 → 층수 저장 → 완료 피드백

### 3-3. 수동 층수 입력 (보조 입력 방식)
- NFC 미지원 단말기 대응
- 메인 화면 차량 카드의 **층수 영역 탭**으로 셀렉터 열기 (별도 버튼 없음)
- 지하층만 선택 (B1 ~ Bn, 설정값으로 관리)

### 3-4. 층수 확인
- 앱 진입 시 메인 화면에서 등록된 차량별 저장 층수 즉시 확인
- 마지막 저장 시각 표시

---

## 4. 사용자 플로우

### 플로우 A — 첫 방문
```
앱 접속
→ 차량 미등록 상태 감지
→ 차량 추가 유도 UI 표시
→ 차량 이름 입력 → 등록 완료
→ 메인 화면 (층수 미저장 상태)
```

### 플로우 B — NFC로 주차 층수 저장
```
주차 완료
→ 엘리베이터 앞 NFC 태그에 폰 태그
→ 앱 자동 실행 (/park?floor=B3)

[차량 2~3대인 경우]
→ "B3에 주차하셨나요?" 팝업 + 차량 선택
→ 차량 선택 → B3 저장 완료 + 진동 피드백 → 메인 화면

[차량 1대인 경우]
→ 자동 저장 → 저장 완료 화면 → 메인 화면 자동 이동 (1.8초)

[차량 미등록인 경우]
→ "차량을 먼저 등록해주세요" 안내 → 메인 화면으로 이동 버튼
```

### 플로우 C — 수동으로 층수 저장
```
메인 화면 진입
→ 차량 카드의 층수 영역 탭
→ 층수 선택 바텀시트 (B1~B8)
→ 저장 완료 + 토스트 피드백
```

### 플로우 D — 차 찾으러 갈 때
```
앱 실행
→ 메인 화면에서 차량별 저장 층수 확인
→ 확인 완료
```

---

## 5. 기술 스펙

### 5-1. 플랫폼
- **PWA** (Progressive Web App)
- 홈 화면 추가 지원 (manifest.json)
- 오프라인 지원 (Service Worker — 정적 에셋 캐싱)

### 5-2. 프론트엔드 스택
| 항목 | 기술 |
|------|------|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일링 | Tailwind CSS v4 |
| 배포 | Vercel |

### 5-3. 저장소
- **브라우저 로컬스토리지** (서버 없음, MVP 한정)
- 향후 v2.0에서 Supabase로 마이그레이션 예정

### 5-4. NFC 방식
- NDEF URL 레코드 방식 (Web NFC API 미사용)
- iOS 14.5+ 백그라운드 NFC 태그 읽기 자동 지원
- Android Chrome 자동 URL 실행 지원
- 태그 1개당 가격: 약 200~500원 (일반 NTAG213 스티커)

### 5-5. 환경변수 (도메인 분리)
```bash
# .env.local (개발)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# .env.production (배포)
NEXT_PUBLIC_BASE_URL=https://myfloor.app
```
→ NFC 태그 URL 생성 시 `NEXT_PUBLIC_BASE_URL` 참조  
→ 도메인 구매 전 Vercel 임시 URL로 개발 진행 가능

### 5-6. 폰트
- **Pretendard Variable** — CDN (jsDelivr) via HTML `<link>` 태그
- 패키지 설치 없이 CDN에서 직접 로드

### 5-7. PWA 아이콘
- Next.js Route Handler + `ImageResponse` (next/og)로 동적 생성
- `/api/icon?size=192`, `/api/icon?size=512` 엔드포인트
- manifest.json에서 해당 URL 참조

### 5-8. 아파트 설정 (화이트라벨 구조)
```typescript
// src/config/apartment.ts
export const APARTMENT_CONFIG = {
  name: "○○ 아파트",
  floors: ["B1","B2","B3","B4","B5","B6","B7","B8"],
  maxVehicles: 3,   // ← v0.3에서 확정
  theme: {
    background: "#F7F3EE",
    surface: "#EDE8E0",
    primary: "#8FAF8B",
    primaryDark: "#6B9166",
    textMain: "#2C2C2C",
    textSub: "#8C8680",
    border: "#D9D2C8",
  }
}
```
→ 타 아파트 배포 시 이 파일만 교체

---

## 6. 데이터 구조 (로컬스토리지)

```typescript
// localStorage key: "myfloor_data"
interface StorageData {
  vehicles: Vehicle[]
}

interface Vehicle {
  id: string           // UUID (crypto.randomUUID)
  name: string         // 사용자 입력 차량명
  floor: string | null // 현재 저장된 층수 (null = 미저장)
  updatedAt: string    // ISO 8601
}
```

---

## 7. UI/UX 요구사항

### 디자인 방향
- 자연소재 미니멀 (Villiv 레퍼런스)
- 모바일 퍼스트 (메인 사용 환경)
- 폰트: Pretendard Variable (CDN)
- 다크모드: 미지원 (MVP 한정, 베이지 라이트 고정)

### 컬러 시스템
| 토큰 | 헥스 | 용도 |
|------|------|------|
| Background | `#F7F3EE` | 앱 전체 배경 |
| Surface | `#EDE8E0` | 카드, 팝업 배경 |
| Primary | `#8FAF8B` | 버튼, 강조 요소, 층수 표시 숫자 |
| Primary Dark | `#6B9166` | 버튼 hover/active |
| Text Main | `#2C2C2C` | 본문 텍스트 |
| Text Sub | `#8C8680` | 보조 텍스트, 시각 정보 |
| Border | `#D9D2C8` | 구분선, 카드 테두리 |

### 메인 화면 구성
```
[ 아파트 이름 + "몇층" 앱명 ]

[ 차량 카드 ]
  차량 이름  [ ✏️ ] [ 🗑️ ]
  ─────────────────────────
  현재 층수 (크게 표시, Primary 컬러) — B3  ← 탭하면 층수 선택 바텀시트
  마지막 저장: 오늘 오후 2:34

[ 차량 카드 ]
  ...

[ + 차량 추가 ] (최대 3대, 3대 등록 시 숨김)
```

### NFC 진입 시 (2대 이상)
```
B3                  ← 층수 크게 표시
"에 주차하셨나요?"

○ 아빠차 (현재 B2)
○ 엄마차

[ 취소 ]  [ 저장 ]
```

### NFC 진입 시 (1대)
```
자동 저장 → 저장 완료 화면 표시 → 1.8초 후 메인으로 이동
```

### NFC 진입 시 (0대)
```
층수 표시
"차량을 먼저 등록해주세요"
[ 차량 등록하러 가기 ]
```

### 수동 층수 변경
- 차량 카드 층수 영역 탭 → 바텀시트 (bottom sheet) 형태로 슬라이드업
- B1~B8 그리드 버튼, 현재 선택된 층수 Primary 색 강조
- 외부 탭 or 취소 버튼으로 닫기

### 피드백
- 저장 완료: 하단 토스트 메시지 ("아빠차 → B3 저장됨") — 2.5초 후 자동 소멸
- 진동 피드백 (Vibration API — Android 지원, iOS 미지원, graceful degradation 처리)
- 차량 삭제: 첫 탭 → "한 번 더 누르면 삭제됩니다" 경고 → 3초 내 재탭 시 삭제

---

## 8. NFC 태그 물리 스펙

| 항목 | 내용 |
|------|------|
| 태그 규격 | NTAG213 (ISO 14443A) |
| 저장 내용 | URL (NDEF Text Record) |
| URL 형식 | `https://myfloor.app/park?floor=B3` |
| 설치 위치 | 각 층 엘리베이터 앞 기둥 또는 벽 |
| 권장 높이 | 110~130cm (성인 폰 태그 편의) |
| 수량 | 지하층 수만큼 (MVP: 8개) |
| 구매처 | 쿠팡 / 알리익스프레스 (NTAG213 스티커) |
| 구매 시점 | 도메인 확정 및 개발 완료 후 |

---

## 9. 향후 로드맵

### v1.1 — 안정화
- 설치 가이드 페이지 (주민 배포용)
- NFC 테스트 페이지 (`/test`)

### v2.0 — 화이트라벨 B2B
- 서버 도입 (Supabase)
- 아파트별 관리자 페이지 (설정 변경, NFC URL 생성)
- 서브도메인 구조 (`apt-name.myfloor.app`)
- 월정액 구독 모델
- 가족 공유 기능 (선택)

### v2.1 — 기능 확장 (검토)
- 주차 히스토리 (날짜별 기록)
- 방문자 주차 메모
- 단지 내 빈 주차 공간 정보 연동 (관리사무소 협의)

---

## 10. 미결 사항

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 1 | myfloor.app 도메인 가용성 확인 | ⏳ 미확인 | 개발 완료 후 구매 |
| 2 | 아파트명/로고 확정 | ⏳ 미확인 | MVP 배포 전 필요 |
| 3 | 관리사무소 NFC 설치 허가 | ⏳ 협의 필요 | |
| 4 | 주민 배포 채널 확정 | ⏳ 미확인 | 관리사무소 공지 또는 단톡방 |

---

*이 문서는 개발 착수 전 요구사항 정의 목적으로 작성되었으며, 구현 중 변경될 수 있습니다.*
